const quarterValue = (wholeNumber) => {
    const quarter = wholeNumber / 4

    return quarter
    // Invoke the function and store the return value
}
const quarteredNumber = quarterValue(400)
console.log(quarteredNumber)

console.log(quarterValue(400))

// Log the return value

